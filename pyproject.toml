[project]
name = "autoresearch-skill"
version = "0.1.0"
description = "基于 autoresearch 框架的自主 Skill 优化工具"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.30.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
