[project]
name = "autoresearch-skill"
version = "0.2.0"
description = "基于 autoresearch 框架的自主 Skill 优化工具 — 通过 opencode 执行任务"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
